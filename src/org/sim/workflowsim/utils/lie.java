package org.sim.workflowsim.utils;

import org.sim.cloudbus.cloudsim.Log;

public class lie {
    public static void main(String[] args) {
        Log.printLine("=================");
        Log.printLine("Pod : pod1 || namespace: default || \nContainer: container1 || image : nginx || cpu : 1 || ram : 10000" );
    }
}
