apiVersion: v1
kind: pod
metadata:
  name: HFSM1
  namespace: default
  labels:
    type: App
spec:
  nodeName: host1
  containers:
  - image: nginx
    name: containerHFSM1
    resources:
      requests:
        memory: 10000Mi
        cpu: 900m
